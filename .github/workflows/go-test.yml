name: go test

permissions:
  pull-requests: read
  contents: read

on:
  push:
    branches:
      - master

  pull_request:

jobs:
  test:
    uses: go-openapi/ci-workflows/.github/workflows/go-test-monorepo.yml@fc0b7d51fa6128079b88acdfe597a8e04495a607 # v0.2.4
    with:
      extra-flags: '-tags testcgo' # this is to trigger extra tests in spew
    secrets: inherit
